import React from 'react';

const Potential = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Potential Projects</h1>
    </div>
  );
};

export default Potential;
